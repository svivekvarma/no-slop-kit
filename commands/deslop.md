---
description: Remove AI-slop patterns from a draft while preserving the writer's voice, using the rules for its channel.
slop-lint: off
---

Edit the draft below to remove AI slop while preserving the writer's voice.

Use the `no-slop` skill. Decide which channel the draft belongs to, read that
one file from `skills/no-ai-slop/channels/`, apply the shared rules in
`skills/no-ai-slop/SKILL.md`, then check your edit against
`skills/no-ai-slop/eval.md` before you answer.

Make the minimum effective edit. Do not invent facts, examples or opinions. If
the draft has no core point you can identify, ask rather than guess.

Return the full edited draft, then a short **What changed** section that names
the channel you applied.

If the text below is empty, ask the user to paste the draft.

$ARGUMENTS
